def auto(): # hier wird die Funktion definiert
    print ("Audi 80") # hier wird der Inhalt der Funktion ausgeführt

auto() # hier wird die Funktion aufgerufen


def addieren(a,b):
    return a + b # hier wird das Ergebnis der Addition zurückgegeben

ergebnis = addieren(3,5) # hier wird die Funktion aufgerufen und das Ergebnis in der Variable ergebnis gespeichert
print (ergebnis)



